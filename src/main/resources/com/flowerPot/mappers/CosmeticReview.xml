<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.flowerPot.cosmeticReview.repository.CosmeticReviewDao">
	
	<insert id="insertcosmeticReview">
		insert into f_cosmeticReview (crno,cno,mno,category,rating,id,nickname,content,title) 
		values (crno_seq.nextval,#{cno, jdbcType=NUMERIC},#{mno, jdbcType=NUMERIC},#{category, jdbcType=VARCHAR},#{rating, jdbcType=NUMERIC},#{id, jdbcType=VARCHAR},#{nickname, jdbcType=VARCHAR},#{content, jdbcType=VARCHAR},#{title, jdbcType=VARCHAR})
	</insert>

	<select id="selectListCosmeticReviewListByCno" resultType="com.flowerPot.vo.CosmeticReviewVo">
		select * from f_cosmeticReview where cno = #{cno}
	</select>
</mapper>
