<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	
	<resultMap type="com.flowerPot.vo.MemberVo" id="memberMap">
		<result property="id" column="id"/>
		<result property="password" column="password"/>
		<result property="name" column="name"/>
		<collection property="authList" resultMap="authMap"></collection>
	</resultMap>
	
	<resultMap type="com.flowerPot.vo.AuthorityVo" id="authMap">
		<result property="id" column="id"/>
		<result property="authority" column="authority"/>
	</resultMap>
	
	<resultMap type="com.flowerPot.admin.vo.EmpVo" id="empMap">
	</resultMap>
	
		<!-- 중복되는 동적 SQL문에 대한 처리 -->
	<sql id="search">
		<!-- 동적 SQL구문 -->
		<if test="condition == 'mno'">
		WHERE mno LIKE '%'||#{keyword}||'%'
		</if>
		<if test="condition == 'id'">
		WHERE id LIKE '%'||#{keyword}||'%'
		</if>
		<if test="condition == 'name'">
		WHERE name LIKE '%'||#{keyword}||'%'
		</if>
	</sql>
	
	<select id="read" resultMap="memberMap">
		select mem.id, mem.password, mem.name, auth.authority from f_member mem left outer join f_authority auth on mem.id = auth.id where mem.id = #{id}
	</select>
	
	
	
	<insert id="insertMember">
		<!-- int mno, String id, String password, String name, String nickname, String address, String email, String gender, String birth, String member_rank, boolean enabled -->
		insert into f_member (mno,id,password,name,nickname,address,email,gender,birth,member_rank,enabled,regdate) values
		(mno_seq.nextval,#{id},#{password},#{name, jdbcType=VARCHAR},#{nickname, jdbcType=VARCHAR},#{address, jdbcType=VARCHAR},#{email, jdbcType=VARCHAR},#{gender, jdbcType=VARCHAR},#{birth, jdbcType=VARCHAR},#{member_rank, jdbcType=VARCHAR},#{enabled, jdbcType=CHAR},sysdate)
		<selectKey resultType="int" keyProperty="mno" order="AFTER">
			select mno_seq.currval from dual
		</selectKey>
	</insert>
	
	
	<!-- <select id="getArticleList" resultMap="memberMap">		
		SELECT * FROM 
		(SELECT rownum rnum, mno, id, name, nickname, address, email, gender, birth, member_rank, regdate, enabled(
		FROM 
		(SELECT * FROM f_member 
		<include refid="search"/>
		 ORDER BY mno DESC))
		WHERE  rnum BETWEEN #{pageStart} AND #{pageEnd}
	</select> -->
	
	
	<select id="selectTotalCount" resultType="int">
		select count(*) from f_member
	</select>

	
	<delete id="deleteMemberById">
		delete from f_member where id=#{id}	
	</delete>
</mapper>